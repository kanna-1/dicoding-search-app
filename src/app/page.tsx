import React from "react";
import { SearchForm } from "@/components/SearchForm";

const MainPage = () => {
    return (
      <div>
        <SearchForm/>
    </div>
    );
};

export default MainPage;
